<%= form_for(@actor) do |f| %>
  <% if @actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@actor.errors.count, "error") %> prohibited this actor from being saved:</h2>

      <ul>
      <% @actor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first, 'First Name' %><br>
    <%= f.text_field :first %>
  </div>
  <div class="field">
    <%= f.label :last, 'Last Name' %><br>
    <%= f.text_field :last %>
  </div>
  <br>
  <%= f.submit class: 'btn btn-success' %>
<% end %>
